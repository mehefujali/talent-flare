

const Loader = () => {
      return (
            <div className=" w-full h-[calc(100vh-340px)] flex justify-center items-center">
                  <span className="loading loading-spinner loading-lg"></span>
            </div>
      );
};

export default Loader;